package fundamentos.controle

/*
    O laço de repetição while pode simular uma uma estrutura definica como
    no for.
 */

fun main() {
    var contador: Int = 1

    while(contador <= 10) {
        println(contador)
        contador++
    }
}